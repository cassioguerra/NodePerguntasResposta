<%- include ('./partials/header.ejs') %>
<body>
    <%- include ('./partials/navbar.ejs') %>
    <div class="container">
        <hr>
        <h1>Perguntas</h1>
        <a href="/perguntas" class="btn btn-primary"> Perguntas </a>
        <hr>
       <% pergunta.forEach(perguntas => {%>
        <div class="card">
            <div class="card-body">
                <h3 class="card-title"><%=  perguntas.titulo %></h3>
            </div>
            <div class="card-footer">
             <a href="/pergunta/<%= perguntas.id %>"> <button class="btn btn-primary">Responde</button></a> 
            </div>
        </div>
       <% }); %> 


    </div>
</body>
<%- include ('./partials/footer.ejs') %>

